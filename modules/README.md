# Nest bindings for Next.js API routes

## Running the app

You can run the Nest API on its own.
From the project root directory, run:

```bash
$ npx nest start modules
```
## Test

```bash
# unit tests
$ npx jest
```
